import styled from "styled-components";

const StyledFitJournay = styled.h1`
  font-family: var(--heading-font);
  font-size: 3rem;
  position: relative;
  left: 30px;
`;

export default function FitJournayHeading() {
  return <StyledFitJournay>FitJournay</StyledFitJournay>;
}
